// import { fromJS } from 'immutable';
// import { makeSelectPhotoPageDomain } from '../selectors';

// const selector = makeSelectPhotoPageDomain();

describe('makeSelectPhotoPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});

// import { fromJS } from 'immutable';
// import { makeSelectRepoPageDomain } from '../selectors';

// const selector = makeSelectRepoPageDomain();

describe('makeSelectRepoPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});

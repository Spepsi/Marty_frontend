// import { fromJS } from 'immutable';
// import { makeSelectInfoPageDomain } from '../selectors';

// const selector = makeSelectInfoPageDomain();

describe('makeSelectInfoPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});

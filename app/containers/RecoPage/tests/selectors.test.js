// import { fromJS } from 'immutable';
// import { makeSelectRecoPageDomain } from '../selectors';

// const selector = makeSelectRecoPageDomain();

describe('makeSelectRecoPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});

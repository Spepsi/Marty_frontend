// import { fromJS } from 'immutable';
// import { makeSelectHistoryPageDomain } from '../selectors';

// const selector = makeSelectHistoryPageDomain();

describe('makeSelectHistoryPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
